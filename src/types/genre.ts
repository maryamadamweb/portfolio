export type GenreMeta = {
  slug: string;
  title: string;
  summary: string;
  cover: string;
  coverWidth: number;
  coverHeight: number;
  coverVideo?: string;
  coverAnimation?: string;
  tags: string[];
};
